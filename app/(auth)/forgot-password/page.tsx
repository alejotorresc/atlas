import { ForgotPasswordForm } from './forgot-password-form';

export default function ForgotPasswordPage() {
  return (
    <div>
      <h1 className="font-display mb-[16px] text-[20px] font-medium tracking-[-0.01em] text-[var(--ds-neutral-900)]">Recuperar contrasena</h1>
      <ForgotPasswordForm />
    </div>
  );
}
